# SourceTailwindPugUsingVWResponsive

Tailwind + Pug + Sass + Responsive "vw"

\*\*init

-   `bower i`
-   `npm install -D @shrutibalasa/tailwind-grid-auto-fit`

-   Source Tailwincss + Gulp 4
-   `npm install` then run `npm run dev`
